import React from 'react';
import { View, Text } from 'react-native';

const Crew = (props) => {
  // props.navigation.navigate('User', {userId: '11111'})
  return (
    <View>
      <Text>Crew list here</Text>
    </View>
  );
};

export default Crew;
