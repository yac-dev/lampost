// main libraries
import React, { useState } from 'react';
import { connect } from 'react-redux';
import { View, Text } from 'react-native';

const ActionButtons = () => {
  return (
    <View>
      <Text>meetup action buttons</Text>
    </View>
  );
};

export default ActionButtons;
