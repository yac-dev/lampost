// main libraries
import React, { useState } from 'react';
import { connect } from 'react-redux';
import { View, Text } from 'react-native';

const Header = () => {
  return (
    <View>
      <Text>meetup header</Text>
    </View>
  );
};

export default Header;
