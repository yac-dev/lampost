import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const Assets = (props) => {
  return (
    <View>
      <Text>Assets</Text>
    </View>
  );
};

export default Assets;
